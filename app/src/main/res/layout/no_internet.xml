<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/no_internet"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:backgroundTint="@android:color/darker_gray">

        <ImageView
            android:id="@+id/no_internet_imageview"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_above="@+id/no_internet_textview"
            android:layout_centerHorizontal="true"
            app:srcCompat="@drawable/ic_baseline_signal_wifi_off_24px" />

        <TextView
            android:id="@+id/no_internet_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignStart="@+id/no_internet_textview2"
            android:layout_centerVertical="true"
            android:text="Nemate internet konekciju."
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/no_internet_textview2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/no_internet_imageview"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="34dp"
            android:text="Spojite se, pa pokušajte ponovno"
            android:textSize="14sp"
            android:textStyle="bold" />

    </RelativeLayout>
</android.support.v4.widget.SwipeRefreshLayout>